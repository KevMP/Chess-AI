#include "UserInput.h"
#include "Chess.h"
#include<vector>

std::vector<sf::Vector2i> chess::Pawn::getPossibleMoves() {
	std::vector<sf::Vector2i> moves;
	//Can go forward 2 if first move
	//can eat accross if not same color
	//can move forward as long as there is no one in front, or king is not in check if they do move



	return moves;
}